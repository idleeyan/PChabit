<?xml version="1.0" encoding="UTF-8" ?>
<UserControl
    x:Class="PChabit.App.Views.WebsiteCategoryTab"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    HorizontalAlignment="Stretch"
    VerticalAlignment="Stretch"
    mc:Ignorable="d">

    <Grid Padding="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- 左侧分类列表 -->
        <Border
            Grid.Column="0"
            Padding="16"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Spacing="8">
                    <TextBlock
                        FontSize="20"
                        FontWeight="SemiBold"
                        Text="网站分类" />

                    <TextBlock
                        FontSize="12"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Text="管理网站分类和域名映射" />
                </StackPanel>

                <Button
                    Grid.Row="1"
                    Margin="0,16,0,0"
                    HorizontalAlignment="Stretch"
                    Content="添加分类"
                    Style="{StaticResource AccentButtonStyle}"
                    Click="OnAddCategoryClick" />

                <ListView
                    x:Name="CategoryList"
                    Grid.Row="2"
                    Margin="0,16,0,0"
                    SelectionMode="Single"
                    SelectionChanged="OnCategorySelectionChanged">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Padding="8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <Border
                                    Grid.Column="0"
                                    Width="32"
                                    Height="32"
                                    Margin="0,0,12,0"
                                    Background="{Binding Color, Converter={StaticResource StringToBrushConverter}}"
                                    CornerRadius="6">
                                    <TextBlock
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontSize="14"
                                        Text="{Binding Icon}" />
                                </Border>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock FontWeight="Medium" Text="{Binding Name}" />
                                    <TextBlock
                                        FontSize="11"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        Text="{Binding Description}"
                                        TextTrimming="CharacterEllipsis"
                                        Visibility="{Binding Description, Converter={StaticResource StringToVisibilityConverter}}" />
                                </StackPanel>

                                <TextBlock
                                    Grid.Column="2"
                                    VerticalAlignment="Center"
                                    FontSize="11"
                                    Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                    Text="{Binding IsSystem, Converter={StaticResource SystemCategoryConverter}}" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <StackPanel Grid.Row="3" Spacing="8">
                    <TextBlock
                        x:Name="TotalCategoriesText"
                        FontSize="12"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Text="共 0 个分类" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- 右侧域名映射 -->
        <Border
            Grid.Column="1"
            Margin="16,0,0,0"
            Padding="16"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- 选中分类标题和操作按钮 -->
                <Grid Grid.Row="0">
                    <StackPanel Spacing="4">
                        <TextBlock
                            x:Name="SelectedCategoryNameText"
                            FontSize="20"
                            FontWeight="SemiBold"
                            Text="选择分类查看映射" />
                        <TextBlock
                            x:Name="SelectedCategoryDescText"
                            FontSize="12"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Text="" />
                    </StackPanel>

                    <StackPanel
                        x:Name="CategoryActionsPanel"
                        HorizontalAlignment="Right"
                        Orientation="Horizontal"
                        Spacing="8"
                        Visibility="Collapsed">
                        <Button
                            Content="编辑分类"
                            Style="{StaticResource AccentButtonStyle}"
                            Click="OnEditCategoryClick" />
                        <Button
                            x:Name="DeleteCategoryButton"
                            Content="删除分类"
                            Click="OnDeleteCategoryClick" />
                    </StackPanel>
                </Grid>

                <!-- 搜索和添加域名 -->
                <Grid
                    x:Name="MappingSearchPanel"
                    Grid.Row="1"
                    Margin="0,16,0,0"
                    ColumnSpacing="8"
                    Visibility="Collapsed">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBox
                        x:Name="SearchBox"
                        Grid.Column="0"
                        PlaceholderText="搜索域名..."
                        TextChanged="SearchBox_TextChanged" />

                    <Button
                        Grid.Column="1"
                        Content="添加域名"
                        Style="{StaticResource AccentButtonStyle}"
                        Click="OnAddMappingClick" />
                </Grid>

                <!-- 域名映射列表或空状态 -->
                <Grid Grid.Row="2" Margin="0,16,0,0">
                    <ListView
                        x:Name="MappingList"
                        SelectionMode="None"
                        Visibility="Collapsed">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Padding="8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock FontWeight="Medium" Text="{Binding DomainPattern}" />
                                    </StackPanel>

                                    <Button
                                        Grid.Column="1"
                                        Content="删除"
                                        Tag="{Binding Id}"
                                        Click="OnDeleteMappingClick" />
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <StackPanel
                        x:Name="EmptyStatePanel"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Spacing="16">
                        <FontIcon
                            FontSize="48"
                            Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                            Glyph="&#xE8FD;" />
                        <TextBlock
                            FontSize="16"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Text="请从左侧选择一个分类" />
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
