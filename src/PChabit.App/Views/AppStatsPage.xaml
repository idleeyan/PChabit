<?xml version="1.0" encoding="UTF-8" ?>
<Page
    x:Class="PChabit.App.Views.AppStatsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border
            Grid.Row="0"
            Padding="24,24,24,0"
            Background="{ThemeResource LayerFillColorDefaultBrush}">
            <StackPanel Spacing="16">
                <TextBlock
                    FontSize="28"
                    FontWeight="SemiBold"
                    Text="应用统计" />

                <NavigationView
                    x:Name="TabNav"
                    PaneDisplayMode="Top"
                    IsBackButtonVisible="Collapsed"
                    IsSettingsVisible="False"
                    SelectionChanged="TabNavigation_SelectionChanged">
                    <NavigationView.MenuItems>
                        <NavigationViewItem Content="应用排行" Tag="Ranking">
                            <NavigationViewItem.Icon>
                                <FontIcon Glyph="&#xE8FD;" />
                            </NavigationViewItem.Icon>
                        </NavigationViewItem>
                        <NavigationViewItem Content="分类管理" Tag="Category">
                            <NavigationViewItem.Icon>
                                <FontIcon Glyph="&#xE8E5;" />
                            </NavigationViewItem.Icon>
                        </NavigationViewItem>
                    </NavigationView.MenuItems>
                </NavigationView>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1" Padding="24" VerticalScrollBarVisibility="Auto">
            <ContentControl
                x:Name="TabContent"
                HorizontalContentAlignment="Stretch"
                VerticalContentAlignment="Stretch">
                <ContentControl.Transitions>
                    <TransitionCollection>
                        <ContentThemeTransition />
                    </TransitionCollection>
                </ContentControl.Transitions>
            </ContentControl>
        </ScrollViewer>
    </Grid>
</Page>
